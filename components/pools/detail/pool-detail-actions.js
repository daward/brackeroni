"use client";
import { useState } from "react";
export function PoolDetailActions({ detail, readOnly, onViewTags }) { const [open, setOpen] = useState(false); const { pool } = detail; const tags = new Set(pool.candidates.flatMap((c) => c.tags || [])).size; return <div className="relative"><button type="button" aria-label="More pool actions" onClick={() => setOpen(!open)} className="flex h-10 w-10 items-center justify-center text-2xl text-[var(--muted)] hover:text-[var(--accent-3)]">{"\u22ee"}</button>{open ? <div className="absolute right-0 top-full z-30 mt-2 w-64 border border-[var(--line-strong)] bg-[var(--panel)] p-2"><Action onClick={onViewTags} disabled={!tags} label="View tags" meta={tags} />{pool.visibility !== "private" ? <Action onClick={detail.copyLink} label="Copy pool link" meta="Share" /> : null}<Action onClick={detail.continueImport} disabled={readOnly} label="Import candidates" meta="Add" /><Action onClick={detail.enrichCandidates} disabled={readOnly || !pool.candidates.some((c) => c.sourceUrl)} label="Enrich from links" meta="Source" /><Action onClick={detail.autoFillMissingImages} disabled={readOnly} label="Fill missing images" meta="Images" /><Action onClick={detail.openMerge} disabled={readOnly} label="Merge another pool" meta="Pick" /><div className="mt-2 border-t border-[var(--line)] pt-2"><Action onClick={detail.archive} disabled={readOnly} label="Archive pool" meta="Hide" /></div></div> : null}</div>; }
function Action({ onClick, disabled, label, meta }) { return <button type="button" onClick={onClick} disabled={disabled} className="flex w-full items-center justify-between px-3 py-3 text-left disabled:opacity-60"><span className="text-sm">{label}</span><span className="text-[11px] uppercase text-[var(--accent-3)]">{meta}</span></button>; }
