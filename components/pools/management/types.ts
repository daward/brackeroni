/**
 * Public contracts for the signed-in pool management workspace.
 *
 * Route hosts do not provide data today; internal list components use
 * management-specific contracts while this public surface stays narrow.
 */
export type PoolManagementWorkspaceProps = Record<string, never>;
